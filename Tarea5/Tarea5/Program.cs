using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Tarea5
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            numerosPares(0, 10);
            Console.ReadLine();
            */
            /*
            decimal resultado = calcularDescuento(100);
            Console.WriteLine(resultado);
            */

            /*
            float sueldoTotal = calcularAumentoSueldo(1500, "A");
            Console.WriteLine(sueldoTotal);
            Console.ReadLine();
            */
        }

        /*
        static void numerosPares(int a, int b)
        {
            for (int i = a; i < b + 1; i++)
            {
                if(a%2 == 0)
                {
                    Console.WriteLine(i);                  
                }                
            }
        }
        */

        /*
        static decimal calcularDescuento(decimal a)
        {
            return (a - ((a * 20) / 100));
        }
        */

        /*
        static float calcularAumentoSueldo(float sueldo, string categoria)
        {
            switch (categoria)
            {
                case "A": return sueldo + 400; break;
                case "B": return sueldo + 200; break;
                default: return sueldo + 50; break;
            }
        }
        */
    }
}
